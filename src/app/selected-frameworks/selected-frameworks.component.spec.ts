/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SelectedFrameworksComponent } from './selected-frameworks.component';

describe('Component: SelectedFrameworks', () => {
  it('should create an instance', () => {
    let component = new SelectedFrameworksComponent();
    expect(component).toBeTruthy();
  });
});
